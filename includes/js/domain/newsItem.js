/**
 * Created by hillga on 9/4/2014.
 */

MYSITE.domain.newsItem = function(title, pubDate, link, content) {
    this.title = title,
    this.publishedDate = pubDate,
    this.link = link,
    this.content = content;
}
